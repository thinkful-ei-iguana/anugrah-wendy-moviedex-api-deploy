Users can:
  search
    by: genre, country, avg_vote (strings, case insensitive)

Endpoint: /movie

search:
  country (use .includes())
  avg_vote ( >= supplied number)

response = [...full movie entries]